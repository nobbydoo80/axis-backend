{% verbatim %}
{{#if help_text}}
    {{#if help_text_inline}}
        <span class="help-block" id="hint_{{this.auto_id}}">{{this.help_text}}</span>
    {{else}}
        <p class="help-block" id="hint_{{this.auto_id}}">{{this.help_text}}</p>
    {{/if}}
{{/if}}
{% endverbatim %}
